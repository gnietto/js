'use strict'

function multiplesOf3and5(number) {
  // un número es multiplo de 3 si la suma de sus dígitos es exactamente divisible por 3
  // un número es multiplo de 5 si el número termina en 0 o 5
  // definir números múltiplos de 3 que son inferiores al argumento 'number'
  // definir números múltiplos de 5 que son inferiores al argumento 'number'
  // definir elementos comunes a ambos cálculos anteriores
  // retornar la suma de todos los números comunes resultantes

}

multiplesOf3and5(1000);

//If we list all the natural numbers below 10 that are multiples of 3 or 5, we get 3, 5, 6 and 9. The sum of these multiples is 23.
//Find the sum of all the multiples of 3 or 5 below the provided parameter value number.
